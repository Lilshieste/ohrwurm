{
  "name": "ohrwurm",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "build": "esbuild web/src/index.js --bundle --outfile=web/bundle.js",
    "watch": "esbuild web/src/index.js --bundle --outfile=web/bundle.js --watch",
    "serve": "npx serve web --symlinks"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.27.3",
    "jest": "^27.3.1",
    "jest-when": "^3.4.2"
  }
}
